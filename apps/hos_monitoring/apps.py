from django.apps import AppConfig

class HosMonitoringConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.hos_monitoring'
    verbose_name = 'HOS Monitoring (FMCSA)'
