from django.apps import AppConfig

class WeightMonitoringConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.weight_monitoring'
    verbose_name = 'Weight Monitoring (Federal Bridge Formula)'
